Đọc project hiện tại. Đây là ValCo TSSS MVP Phase 2 Ready V3.1.

Chỉ build Phase 3: backend API routes còn thiếu + frontend dashboard tối thiểu.
Không hỏi linh tinh. Không gọi provider trực tiếp. Không để LLM chọn final 3.
Chạy pytest, ruff check và ruff format --check sau khi sửa.
